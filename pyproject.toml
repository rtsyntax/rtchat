[project]
name = "rtchat"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "channels-redis>=4.3.0",
    "channels[daphne]>=4.3.1",
    "django>=5.2.6",
    "psycopg2>=2.9.10",
]

[tool.uv]
package = true
